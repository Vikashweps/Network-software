type Product {
  id: ID!
  name: String!
  price: Float!
  inStock: Boolean!
}

input ProductInput {
  name: String!
  price: Float!
  inStock: Boolean = true
}

type Query {
  products: [Product!]!
}

type Mutation {
  createProduct(input: ProductInput!): Product!
}